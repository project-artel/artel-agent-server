class ScreenVerdictError(RuntimeError):
    """모델이 형식을 어겨 판정을 읽을 수 없다.

    부르는 쪽은 이것을 **항목 없는 답**으로 옮긴다. 스키마를 채우려고 항목을 지어내지
    않는다 — 기본값이 무시라 빈 답은 지도를 종전대로 두지만, 지어낸 항목은 그 `scene` 의
    화면을 갈라 놓고 되돌릴 수 없다.
    """
